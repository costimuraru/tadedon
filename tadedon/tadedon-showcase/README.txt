This project runs on Google App Engine.

In order to start app engine from eclipse, it should be unpacked first. Here
is the command:

mvn -DunpackVersion=1.7.7.1 net.kindleit:maven-gae-plugin:unpack

Where unpackVersion parameter is the gae version specified in pom.
